<h1>Listing units</h1>

<table>
  <tr>
    <th>Number</th>
    <th>Program</th>
    <th>Organization</th>
    <th>District</th>
    <th>Meeting address</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @units.each do |unit| %>
  <tr>
    <td><%= unit.number %></td>
    <td><%= unit.program_id %></td>
    <td><%= unit.organization_id %></td>
    <td><%= unit.district_id %></td>
    <td><%= unit.meeting_address %></td>
    <td><%= link_to 'Show', unit %></td>
    <td><%= link_to 'Edit', edit_unit_path(unit) %></td>
    <td><%= link_to 'Destroy', unit, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Unit', new_unit_path %>
